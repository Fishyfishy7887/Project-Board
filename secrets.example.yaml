# Copy this file to secrets.yaml. Never commit the populated secrets.yaml.
wifi_ssid: "YOUR_WIFI_NAME"
wifi_password: "YOUR_WIFI_PASSWORD"
# ESPHome requires at least eight characters for the fallback access point.
fallback_ap_password: "CHANGE_ME_NOW"
