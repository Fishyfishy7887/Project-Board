import { readFileSync, readdirSync, statSync } from 'node:fs';import { join } from 'node:path';
const files=[];function walk(d){for(const f of readdirSync(d)){if(['node_modules','.next','.git'].includes(f))continue;const p=join(d,f);statSync(p).isDirectory()?walk(p):files.push(p)}}walk('.');let bad=[];for(const f of files){if(!/\.(html|tsx|ts|css)$/.test(f))continue;const s=readFileSync(f,'utf8').toLowerCase();if(s.includes('lorem ipsum'))bad.push(`${f}: lorem ipsum`);if(/(?<!no |not |does not )guaranteed income/.test(s))bad.push(`${f}: unsupported guaranteed income claim`);if(s.includes('make money instantly'))bad.push(`${f}: unsupported instant-money claim`);if(s.includes('secret ai loopholes'))bad.push(`${f}: unsupported loophole claim`);if(s.includes('fake testimonial'))bad.push(`${f}: fake testimonial marker`)}if(bad.length){console.error(bad.join('\n'));process.exit(1)}console.log('Content check passed: no banned placeholder or unsupported claim phrases found.');
